{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**", "esm/**"],
      "dependsOn": ["^build"]
    },
    "clean": {},
    "changelog": {}
  }
}